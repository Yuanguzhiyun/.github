{
  "name": "Python CI (uv + pytest, self-hosted)",
  "description": "setup-python + setup-uv + uv sync + pytest; runs on racknerd self-hosted runner. Adjust uv sync flags / pytest target per project layout.",
  "iconName": "octicon code",
  "categories": ["Continuous integration", "Python"]
}
